{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions for every window. Each window is listed because each is told what to show by an event, and a window outside every capability cannot listen for one: its listener is refused silently, and it never learns what it is for.",
  "windows": [
    "main",
    "gate",
    "notice"
  ],
  "permissions": [
    "core:default"
  ]
}
