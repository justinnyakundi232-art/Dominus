{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions for both windows. The gate is listed because it is told what was blocked by an event, and a window outside every capability cannot listen for one: its listener is refused, it never resets, and its buttons stop answering after the first time.",
  "windows": [
    "main",
    "gate"
  ],
  "permissions": [
    "core:default"
  ]
}
